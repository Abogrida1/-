# نظام تسجيل الدخول

## نظرة عامة
تم إنشاء نظام تسجيل دخول كامل بدون تسجيل الدخول بجوجل. النظام يدعم:
- تسجيل الدخول الطبيعي
- التسجيل بخطوات متعددة
- صفحة ترحيب
- معالجة المعامل `google=true` في URL

## الملفات
- `/auth/login.html` - صفحة تسجيل الدخول
- `/auth/register.html` - صفحة التسجيل (3 خطوات)
- `/auth/welcome.html` - صفحة الترحيب بعد التسجيل
- `/auth-script.js` - منطق تسجيل الدخول والتسجيل
- `/auth-styles.css` - أنماط صفحات تسجيل الدخول

## الميزات

### تسجيل الدخول
- تسجيل الدخول بالبريد الإلكتروني وكلمة المرور
- خيار "تذكرني"
- رابط لاستعادة كلمة المرور
- التحقق من صحة البيانات

### التسجيل
نظام التسجيل بثلاث خطوات:
1. **الخطوة 1**: المعلومات الأساسية (الاسم، البريد، الهاتف)
2. **الخطوة 2**: تفاصيل الحساب (كلمة المرور، تأكيد كلمة المرور، الموافقة على الشروط)
3. **الخطوة 3**: التحقق من البريد الإلكتروني (رمز التحقق)

### معالجة المعامل `google=true`
عند الوصول إلى `/auth/register.html?step=1&google=true`:
- يتم إزالة المعامل `google=true` تلقائياً من URL
- يتم توجيه المستخدم إلى التسجيل الطبيعي
- لا يوجد أي تأثير على عملية التسجيل

## التخزين
يتم حفظ بيانات المستخدمين في `localStorage` تحت المفتاح `registered_users`.

## الجلسات
- إذا تم تحديد "تذكرني": يتم حفظ الجلسة في `localStorage`
- إذا لم يتم تحديد "تذكرني": يتم حفظ الجلسة في `sessionStorage`

## الوظائف المتاحة

### `checkAuth()`
التحقق من وجود جلسة نشطة للمستخدم.

### `logout()`
تسجيل الخروج وحذف الجلسة.

### `showNotification(message, type)`
عرض إشعار للمستخدم.
- `type`: يمكن أن يكون `success`, `error`, `warning`, `info`

## الروابط
- تسجيل الدخول: `/auth/login.html`
- التسجيل: `/auth/register.html?step=1`
- صفحة الترحيب: `/auth/welcome.html`

## ملاحظات
- لا يوجد أي كود متعلق بتسجيل الدخول بجوجل
- جميع الروابط تستخدم المسارات المطلقة (`/auth/...`)
- النظام جاهز للربط مع نظام الحضور والانصراف
